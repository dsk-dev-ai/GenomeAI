# `plugins/` — Plugin System

Community-contributed plugins that extend GenomeAI capabilities.

Each plugin is a self-contained module that integrates via the plugin hook system. Plugins can add custom analysis steps, annotation sources, ML models, and more.

See [docs/plugins/](../docs/plugins/) for plugin development guides.
