# `docker/` — Docker Configuration

Dockerfiles, entrypoint scripts, and compose overrides for the GenomeAI platform.

Contains:
- Service-specific Dockerfiles (built as apps reach implementation)
- Docker Compose override files for different environments (dev, staging, production)
- Health check scripts
- Init scripts for development databases
