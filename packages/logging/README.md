# `packages/logging` — Logging Library

Centralized structured logging for all GenomeAI services.

Provides `genomeai-logging` Python package with:
- `setup_logging()` — Configure logging (console or JSON format)
- `get_logger()` — Get a logger instance
- `_JsonFormatter` — JSON formatter for production use
