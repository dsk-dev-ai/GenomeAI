# `packages/ui` — UI Component Library

Shared React component library used by the web app and desktop app.

**Planned stack:** React, TypeScript, Tailwind CSS, Storybook

This package will contain reusable UI primitives: buttons, forms, tables, charts, and layout components.
