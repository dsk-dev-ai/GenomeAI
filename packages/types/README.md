# `packages/types` — Shared TypeScript Types

Shared TypeScript type definitions used across the monorepo.

Contains interfaces, enums, and type utilities that ensure consistency between apps and packages.
