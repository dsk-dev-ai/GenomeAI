[project]
name = "genomeai-sdk"
version = "0.1.0"
description = "Python SDK for GenomeAI"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/genomeai_sdk"]
